# Copy to .env and fill the values
PORT=4000
DATABASE_URL=postgresql://postgres:postgres@db:5432/clinic_db?schema=public
JWT_SECRET=replace_me_with_a_secure_value
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
